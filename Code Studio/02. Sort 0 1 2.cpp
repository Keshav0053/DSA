#include <bits/stdc++.h> 
void sort012(int *arr, int n)
{
   //normal array
   sort(arr,arr+n);
   //sort(arr,arr+n,greatest<int>());
}
//vector
//  sort(arr.begin(),end()
//sort(arr.begin(),end(),greatest<int>());
